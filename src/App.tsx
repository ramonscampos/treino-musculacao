export function App() {
  return (
    <div className="min-h-dvh" style={{ background: 'var(--bg-color)', color: 'var(--text-primary)' }}>
      <p className="p-8 text-center" style={{ color: 'var(--text-muted)' }}>Iron Protocol — carregando...</p>
    </div>
  )
}
